# JoinTheParty

## Agent skills

### Issue tracker

GitHub Issues on RBILLC/JoinTheParty via the `gh` CLI; `backlog-tickets.md` is historical only. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` at the repo root, ADRs in `docs/adr/`. See `docs/agents/domain.md`.

### Build environment

Exact toolchain paths, build dirs, and the traps (DLL PATH rule, gradle UP-TO-DATE, test counts): `docs/agents/build-environment.md`. Read it before building core or android.
