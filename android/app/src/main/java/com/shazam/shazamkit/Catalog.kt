package com.shazam.shazamkit

/**
 * STUB — compile-time stand-in for the Apple ShazamKit Android AAR (NAT-06).
 * Delete when the real dependency is vendored; call sites must not change.
 *
 * Mirrors `com.shazam.shazamkit.Catalog`: an opaque handle to the catalog a
 * [StreamingSession] matches against. No members — real ShazamKit exposes
 * catalog customization (custom catalogs) that this app doesn't use (v1
 * matches against Shazam's default global catalog only).
 */
interface Catalog
